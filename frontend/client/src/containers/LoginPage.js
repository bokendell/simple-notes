import Layout from 'components/Layout';

const LoginPage = () => {
    return (
        <Layout title='Simple Notes | Login' content='Login Page'>
            <h1>Login</h1>
        </Layout>
    );
};

export default LoginPage;